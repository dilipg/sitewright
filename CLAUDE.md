# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Product summary

**Sitewright** — an AI website generator (renamed from `website-generator` on 2026-09-02; the npm scope is `@sitewright/*`). A user types a one-line brief, an agent pipeline generates a complete multi-page marketing/storefront/SaaS site as typed React source, the user edits it on an infinite canvas (text, style, layout, visibility) and regenerates individual sections without losing edits, then exports a zip of developer-handover-quality code that matches the preview **pixel-for-pixel on every edited node**. The product's one unforgivable failure is preview ≠ handover; the entire architecture (stable node IDs, an override layer, a deterministic exporter) exists to prevent it.

## Current state

**Milestones 1–6 are complete.** (prompts P0–P2, 1.1–1.5, 2.1–2.4, 3.1–3.4, 4.1–4.3, 5.1–5.4). The walking skeleton works end to end (brief → generated hero → canvas edit → export; [m3-soak.md](docs/reports/m3-soak.md): 10/10 at 0.24× budget), section regeneration works via Kitaru replay-with-overrides with gate 7 + the orphan dialog + revert, and **the stop-the-line risk is retired**: the ID-survival stress suite ([m4-id-survival.md](docs/reports/m4-id-survival.md)) measured 100% auto-reattach (71/71) with zero silent drops across 20 varied regen instructions. Milestone 5 built out the full pipeline: intake + planner + plan approval (5.1), the Design System Agent (5.2), the Shell Agent + parallel page fan-out (5.3), and 5.4 — the archetype catalog grew to six (hero, feature-grid, cta-band, pricing-tiers, faq-accordion, social-proof), the editor gained its remaining P0 edit channels (text, layout, visibility) alongside the existing style channel, and the canvas became a real DOM-based, CSS-transformed infinite pan/zoom stage (one live iframe per route, virtualized to a placeholder beyond the render-ahead range — measured 60.0fps for both pan and zoom against a real 4-route generated site, [m5-canvas-perf.md](docs/reports/m5-canvas-perf.md)). The invariant suite now covers the full 4-channel × 6-archetype matrix (28 cases). 5.4 also surfaced and fixed two major, previously-invisible gaps: no generated project had real client-side routing (`main.tsx` was static, always rendering the home page regardless of URL — fixed via `react-router-dom` + route-agnostic page discovery in the scaffold every project copies from), and the exporter could not compile ANY override (text/style/layout/visibility) on a list-item node — a pricing tier, FAQ item, feature card, or testimonial — since its node id only ever exists as a template-literal pattern, never a literal JSX attribute; fixed via a new contract convention (5.5: exporter-only `className`/`childClassNames`/`hidden`/`childHidden` fields on every list item's data shape) plus matching exporter logic and archetype-template updates. A genuine data-loss bug was also found and fixed in the editor's debounced persistence (a redundant "startup save," triggered by bootstrap's own initial history hydration, could race and lose a fast first edit). See decisions.md's 2026-07-28 rows for the full account of all of the above, plus the earlier 5.1–5.3 gap cluster (gate 4 list-item ID recognition, `skipMissingCheck` scoping, tool-use schema defenses, the Kitaru `run_id` reuse lesson). `compiler/` holds the deterministic spine (token deriver, manifest service incl. replaceSection + a cross-process file lock + targeted rollback-commit, gates 1–7 + CLIs, exporter incl. list-item override compilation, bridge shim + preview server with overrides/regen/plan APIs); `editor/` covers selection, all four P0 edit channels, persisted undo/redo, the regen UX, and the infinite canvas; `orchestrator/` (Python/uv/Kitaru, needs `orchestrator/.env` with `ANTHROPIC_API_KEY`; `GEMINI_API_KEY` + `ORCH_MODEL_PROVIDER=gemini` is an opt-in escape hatch, default stays Anthropic) holds prompts/runlog/generation/regeneration/soak/stress/shell/fan-out. 5.5 built the first-ever end-to-end orchestration entry point (`orchestrator/src/orchestrator/acceptance.py`, chaining plan→approve→design→shell→fan-out→export in one process — every stage had only ever been a separately human-invoked CLI before) and a dollar-cost calculator (`orchestrator/src/orchestrator/pricing.py` — no prior measurement had ever converted tokens to dollars), then ran it live: a cheap single-page proof pass followed by 3 full 3-page acceptance runs, per [m5-acceptance.md](docs/reports/m5-acceptance.md). Cost passed comfortably on all 3 runs (~$1.7–1.8 vs. the $10 target/$15 ceiling); wall clock did not (0/3 inside the 5-minute target, 2/3 inside the 10-minute ceiling — fan-out's real per-section generation latency runs ~1.6–2.4× the docs' own ~55s/section assumption, not from prompt bloat), reported honestly rather than rounded up, per explicit human sign-off to not remediate within this milestone. The very first live, full, end-to-end runs surfaced five more real bugs, all fixed: two in the `generic-section` fallback template that only a real (non-fixture, non-6-archetype) generation run could ever trigger (contract 5.6 — a section's `<SectionName>Props` must never declare `nodeId`, it comes from a separate `NodeProps` intersection; contract 4.1 — primitives are default exports, stated nowhere before), a manifest-registration gap for `FailedSectionPlaceholder` (the first-ever live retry-exhaustion), the same template's missing list-item node-id worked example, and — most severe — an unhandled `KeyError` in `write_section_only` on a missing `sectionMeta` that could crash an entire page-worker process and silently drop every remaining section on that route, fixed via the same defensive-accessor pattern already used for a different required field. Edit-across-channels + regen was reconfirmed against real generated content (not just the fixture): all 4 P0 channels compiled correctly on a live project, and a real regen call preserved all 4 overrides with zero orphans (79.4s, over the 60s target/under the 90s ceiling). Full account: decisions.md's 2026-07-29 rows. **Milestone 6 (hardening + handover quality) is complete.** 6.1 grew the catalog to the full 20 archetypes (marketing/storefront/SaaS sets), each soaked 5 runs; 6.2 built the handover package (`HANDOVER.md` generation, a deterministic byte-identical zip, override archiving, and the editor's export UI with a loud gate-report failure panel); 6.3 covered every row of the pipeline's section-8 failure table with automated tests ([m6-failure-drill.md](docs/reports/m6-failure-drill.md)) and replaced the flat run log with a DAG timeline report (`orchestrator.run_report`: per-node status/cost/latency, drill-down to the exact rendered prompts and raw output); 6.4 ran the developer-handover trial twice against cold, no-context sessions given only an export zip ([m6-handover-trial.md](docs/reports/m6-handover-trial.md)) — **both runs met the exit criterion with zero generated components rewritten**, and the wired-up cart ran against a live REST API in each. Milestone 6 found bugs no gate could have caught, because all of them compile cleanly: two shipped defects in nine lines of generated shell (raw `<a href>` internal nav, so every click was a full page reload destroying React state; plus a dead link to the literal URL `/product/:id`), every generated site shipping the FIXTURE's identity (`<title>Acme Analytics</title>`, `"name": "acme-landing-fixture"`), an empty cart rendering a live checkout button, and `HANDOVER.md`'s central claim about swapping mock data for an API being flatly impossible as written. It also closed a real gap in gate 1, which contract section 8 defines as "imports resolve; **build passes**" but which only ever did the first half — a section that used an undeclared field passed all gates, all four page workers exited 0, and the run died at the export's typecheck after full spend; gate 1 now runs the project's own `tsc --noEmit` (1.4s, route-scoped so it is safe under parallel fan-out). Full account: decisions.md's 2026-07-30 rows.

**Milestone 7 (polish + P1s) is COMPLETE** (7.1-7.9, each written up as an executable mini-prompt in [docs/build-prompts-v1.md](docs/build-prompts-v1.md) before its implementation, per the milestone-7 process). The correctness work came first: 7.1 fixed section regeneration, which could not run at all for 19 of 20 archetypes (Kitaru substitutes `${...}` as env vars in replayed values, and contract 5.2 requires `${nodeId}` in list-item ids) plus a second bug where `recorded_exec_id` replayed whichever section finished last rather than the one requested; 7.2 scoped gates 1/2/3/5/6 to the route under generation so a worker cannot burn its retry budget on a sibling's half-written page; 7.3 measured wall clock and found 5.5's diagnosis wrong -- per-section latency is 27s, not the 88-132s inferred, the run completes in 286s inside the 5-minute target, and the real costs are a ~149s sequential prelude plus ~13s/section of contention inflation ([m7-wall-clock.md](docs/reports/m7-wall-clock.md)); 7.4 closed the four two-trial handover findings (money as numbers behind `src/lib/format.ts`, a `Notice` primitive for runtime status, per-item pending, derived-type guidance). Then the P1 features, cheapest blast radius first: 7.8 responsive read-only preview (read-only because an override carries no breakpoint, contract 6.1); 7.7 image replace as the TEXT channel with key `src` (PRD 3.5: content, not style -- not a new channel); 7.5 section reorder, the first PAGE-level override channel, whose `nodeId` is a route slug because reordering belongs to the page rather than to any section -- strict by design (a partial order is a hard export failure, since an omitted section would silently vanish), running last of all channels (visibility removes JSX first), leaving id-less children like `FailedSectionPlaceholder` in place, and reordering the REAL DOM in preview rather than faking it with flex `order`, which would change the formatting context and break preview = handover invisibly; 7.9 page-level regeneration as a sequential loop over the section path (there is no page-level execution to replay, per 7.1) with one route-wide snapshot taken before the first section, so one revert restores the page; 7.6 add-a-section last, since it spends budget per use and mutates the site plan -- a first generation (not a replay: the section never existed) appended to the source and positioned by a `sectionOrder` override, which is why it needed 7.5. Milestone 7's own findings are in decisions.md's 2026-08-02 rows; three are worth knowing: reordering `home` in the invariant suite flipped two unrelated cases onto different pixel boundaries, because a ghosted node legitimately shifts everything below it between preview and export, so the reorder case lives on the one route with no visibility case; the `+` affordance first shipped as a full-width invisible strip that swallowed clicks on section content; and mock regen hardcoded `Hero`, so a page loop would have rewritten one file six times while reporting six sections done. **Two verification gaps are recorded deliberately rather than implied: neither 7.9 nor 7.6 was run against a live model** -- both wrap flows already proven live, and both are covered by unit tests plus mock-mode e2e (7.6's ends in a real export, so all seven gates run). (The "full 15-primitive set" doc drift this paragraph once flagged was **resolved on 2026-08-02**: both docs now name contract 4.1's minimum set plus 7.4's `Notice`. The contract was never violated -- 4.1 says "Minimum v1 set".)

**Post-milestone-7 work is organised as five SLICES turning this local single-user tool into a hosted multi-user app, specced in [docs/superpowers/specs/2026-08-04-accounts-byok-tenancy-design.md](docs/superpowers/specs/2026-08-04-accounts-byok-tenancy-design.md) (13 decisions, 4 named accepted risks). Slices 1, 2, 3 and 4 are merged except for 4c (the preview pool); slice 4 was split into 4a tenancy, 4b spend cap, 4c preview pool.** Slice 1 (prompt-driven editing) added `/__edit-prompt`: the user types an instruction instead of clicking, and an edit agent returns override operations. Slice 2 (accounts) added a **fourth workspace package, `server/`** — the one thing a fresh session is least likely to look for, because milestones 1–7 never mentioned it. `server/` is a **separate composition root**, not a change to the existing one: `compiler/scripts/preview.ts` stays unauthenticated and local, all pre-existing tests stay untouched, and auth is added by composing a different root rather than by editing any handler. It holds a SQLite identity store on `node:sqlite` (no `better-sqlite3`; WAL, foreign keys on, `busy_timeout` because the CLI and the server are two processes on one file), argon2id passwords, server-side sessions, a hand-rolled `node:http` route table where **the table IS the allowlist** (an unregistered path is unreachable, not merely unguarded), `POST /api/login` + `/api/logout` + `GET /api/me`, and an operator CLI that is the **only** way an account comes into existence — invite-only is structural, not an unimplemented feature. Three properties are load-bearing and easy to break by accident: the auth failure response is **uniform** across unknown-email / wrong-password / disabled (else the form is an enumeration oracle, which is why a real argon2 verification runs even when no user matched); revocation is **immediate** (the CLI flags the account *and* hard-deletes its session rows, while `resolveSession` independently re-reads the user and rejects a disabled one — either alone would do, both are deliberate); and login **requires `Content-Type: application/json`**, which is what closes login-CSRF, since `SameSite=Lax` only guards requests that *send* the cookie and login *sets* one. The whole-branch review found four defects no per-task review could see, all fixed — worth knowing because they are the shape of bug this package attracts: a malformed `Host` header **killed the entire process** (the `new URL()` call sat outside the try, so the rejection went unhandled); `busy_timeout` was 0, so an emergency `disable` could silently fail against a running server; login was CSRF-able; and the identity database was not gitignored. Fixing the first then introduced a fifth — module-scope `uncaughtException` handlers swallowed *startup* failures and exited 0 reporting success — so those handlers now arm only inside the `listen` callback. Full account: decisions.md's 2026-08-03 and 2026-08-04 rows.

**Slice 3 (BYOK) and slices 4a (tenancy) + 4b (spend cap) are merged.** 3 stores a per-user Anthropic key encrypted with AES-256-GCM under a master key that lives **only** in `WEBGEN_MASTER_KEY` — the app refuses to boot without it, and `serve.ts` deletes the variable from `process.env` immediately after reading it, because `compiler/`'s three spawn sites (`vite build`, `tsc --noEmit`, the regeneration subprocess) pass no `env` and would otherwise inherit it into model-generated build config. Only a last-4 fingerprint ever reaches the UI, and `getApiKeyFingerprint` takes no master key at all, so a display-only caller is structurally incapable of decrypting. 4a gives every project exactly one owner: `requireProject` composes over `requireSession` and answers a foreign project and a nonexistent one **identically** (404, one shared constant, one branch) so no endpoint becomes an enumeration oracle; `project-registry.ts` is a **partition of the live route table**, so an endpoint added without an authorization rule fails a test. 4b adds `usage_event` and the cap: `checkSpendCap` reports cap/spend/**an exactly computed reset instant** (in-window events dropped oldest-first until the remainder clears the cap — "oldest + 24h" is wrong whenever dropping one event is not enough), and `requireBudget` refuses with **402, not 429**, because every client library retries a 429 and retrying cannot help until the window rolls. The orchestrator now prices each `usage.jsonl` row (`cost_usd`, and **NULL rather than 0.0** for a model with no published rate) and honours `WEBGEN_USAGE_LOG` so one invocation's spend can be attributed to one user; `fixtures/usage-log-contract.jsonl` is a machine-generated golden file pinning the row shape from both languages, because the Python writer and the TypeScript reader were built from separate briefs and nothing else proves they agree. Full account: decisions.md's 2026-08-04 and 2026-08-05 rows.

**Slice 4c was split in two, and both halves are merged. Only slice 5 remains.** 4c-1 spawns one Vite dev server per open project as a **child process** — never in-process, because the server holds the master key and the child loads the project's own unvalidated `vite.config.ts` — on a port the **parent** probes and passes concretely (Vite treats `port: 0` as "no port configured" and falls back to its own default, so every child asking for 0 collides), reverse-proxies it at `/preview/<projectId>/*` behind `requireProject`, reaps it when idle unless work is in flight, and hard-caps at 6. The child's `PREVIEW_READY` line is a **readiness hint whose announced port is discarded** — it arrives on the stdout of a process running unvalidated code, so believing it would hand untrusted input a redirect primitive; readiness is confirmed with a real TCP connect instead. The forwarded path is the request's **original `req.url`**, prefix and query intact: Vite given a non-root `base` expects the prefix present, and stripping it makes Vite 302 back to the prefix just removed, looping the client. `buildAgentEnv` and `redactSecrets` finally have callers.

Three findings from 4c-1 are worth knowing because none of them could fail a test. The preview initially served a page with **no content at all** — `<BrowserRouter>` had no `basename`, so under `/preview/<id>/` React Router matched nothing and rendered `null` while the app shell still drew the nav and footer: HTTP 200, a loaded bridge shim, 0 `[data-node-id]` nodes against 77 at base `/`, and all four P0 edit channels inert. Vite's dev server also **served files across tenants**: `searchForWorkspaceRoot` walks up to the repo root, so an authenticated owner of one project could read another project's overrides byte-identically, and `fs.deny` does not cover `*.db` while the identity store lives inside that root. And the WebSocket upgrade handler is **the one authorization path the route table cannot cover** (an upgrade fires `'upgrade'`, never `'request'`, so `requireProject` never runs), which is why it was extracted into its own module — inline in `serve.ts` it was untestable, and deleting its ownership comparison left the whole suite green. Full account: decisions.md's 2026-08-05 rows.

Two 4c-1 properties still bind anything built on top: a plugin's `configureServer` middleware registers **before** Vite's own base middleware, so a child's `/__*` handlers only match an *unprefixed* URL (which is why the compiler endpoints proxy at `/__regen`, not `/preview/<id>/__regen`); and an open HMR socket deliberately does **not** count as "use", so a child can be reaped under a live socket and nothing respawns until the next HTTP request.

**4c-2 mounted all fifteen `/__*` (method, path) entries** — the spec names ten, the code has fifteen — each behind `requireProject`, with the four that spawn the orchestrator (`/__regen`, `/__regen-page`, `/__add-section`, `/__edit-prompt`) additionally behind `requireBudget`. `/__export` and `/__export-download` are deliberately **not** capped: the exporter is deterministic, and refusing an export over the cap would strand a user's work behind a bill. The endpoint list is **derived from `project-registry.ts`**, never retyped, and the registry's billable-enforcement test iterates *both* lists — the project-scoped and the session-only — because slice 5's generation endpoint is session-only and would otherwise mount with no cap test at all. Spend attribution rides an **opaque 32-hex id** in a header (never a path, so a subprocess's write location is not client-selectable); ingest runs after the response, exactly once, then deletes the file, in the **same `finally` as `pool.release`** so a run that *failed* is still billed — but is skipped when the exchange never completed, because `proxyHttp` resolves early on its 15-minute timeout (or a dead/erroring upstream) while `uv run python` keeps writing. A client abort does **not** resolve it early any more (`fix/proxy-residuals`, below). Typed key errors map at the boundary (missing key 400, disabled 403, undecryptable 500 + log), and a per-user in-flight reservation bounds concurrent billable starts at 2 with **429, not 402** — retrying *does* help once the in-flight run finishes.

Three 4c-2 findings are worth knowing, all from the whole-branch review and none catchable by a task-scoped one. A proxied `route` field was joined into a filesystem path unvalidated, and `path.join` normalises `..` — so an invited user could copy another tenant's `src/` into their own `.regen-backup/` and read it through their *own* authorised preview. A **warm child keeps the key it was spawned with**, while `requireApiKey` checks the database: the ordinary first-run sequence (open with no key → keyless child → save key → regen) sent work to that keyless child, and python-dotenv's `override=False` let an *absent* injected key fall through to `orchestrator/.env`, so the **operator** paid for a run recorded against the user. The pool now respawns a child whose key fingerprint has gone stale — except when it has work in flight, where it serves the stale key rather than killing a paid run. And ingest on an aborted request captured a *partial* log and deleted it, after which the orchestrator recreated the file and wrote the rest where nobody would read it. Full account: decisions.md's 2026-08-06 rows.

**Both named 4c-2 residuals are fixed (`fix/proxy-residuals`).** Residual 1 — `pool.release`/the billable in-flight slot freeing while a paid run was still going — is closed at the source: on a client abort, `proxyHttp` no longer destroys the upstream request or settles early; it lets the child run to completion (it has no abort listener of its own regardless) and discards the bytes once nobody is listening, settling `completed: true` only once the child genuinely finishes, so `release`/`releaseBillableSlot` now free honestly and an aborted-but-completed run still gets billed. The observable cost, recorded rather than hidden: an aborted request can now hold a loopback connection to the child open for up to the full 15 minutes of `PREVIEW_PROXY_TIMEOUT_MS`, where it used to close within one tick — a genuine timeout is unchanged (still destroys, still 504, still skips ingest). Residual 2 — a proxied body bypassing `router.ts`'s `MAX_BODY_BYTES`, with the failure landing as unbounded **memory** in the preview child before ever disk — is closed where the memory actually accumulates: `compiler/src/preview.ts`'s `handleFileEndpoint` and `compiler/src/regen-api.ts`'s `readBody` each answer 413 past a shared `compiler/src/max-body-bytes.ts` constant (the same 1,000,000 figure `router.ts` uses, independently defined since `compiler/` has no dependency on `server/`, and pinned equal by a test), before ever calling `JSON.parse`; `proxyHttp` deliberately gains no cap of its own, since it only pipes and its own memory is already bounded by backpressure. (Both of this paragraph's former "still open" items are now CLOSED: the editor calls `/__archetypes` through `backend.apiUrl`, so it carries `?project=`, and the editor IS wired to the hosted server -- slice 5's hosted mode. Verified in round 1.)

**Slice 4b's four carry-forward items are all closed by 4c**: `requireBudget` and `ingestUsageLog` have callers, ingest happens exactly once per invocation from a unique path, the typed key errors map to statuses, and the concurrent-start multiplier is bounded by a per-user reservation. `redactSecrets` is no longer inert either.

**Slice 5 (the job model + web-triggered generation) is COMPLETE**, specced in [docs/superpowers/specs/2026-08-06-job-model-design.md](docs/superpowers/specs/2026-08-06-job-model-design.md). Every long-running operation is now a **server-side job**: enqueued, worked by an in-process loop, polled by the browser. A job is a wrapper around the same work the server already did, which is what kept it small — five kinds replay a proxied request to the project's preview child (unchanged, still synchronous), and only `generate` is different, spawning the orchestrator directly because there is no site for a Vite child to serve until it has run. `POST /api/generate` is **the first route in this codebase that creates a project** (the row *and* its directory, before the job is queued, so a failed generation leaves an owned, deletable project rather than an orphan) — and `user-cli.ts` is still the only thing that creates a *user*. **A live generation was run end to end: 401.8s, $1.09, 10/10 sections, all 15 `usage_event` rows attributed to the right user, ingested cost bit-for-bit equal to the orchestrator's own total.**

Four things about slice 5 bind anything built on it. **The per-user billable enqueue bound is atomic with the insert** (`createBillableJobIfUnderBound`, one `INSERT … SELECT … WHERE count < ?`) — a check-then-insert across an `await` let 10 concurrent requests pass a bound of 2, and that is *not* a hypothetical: it was measured. Its race-freedom is proven **by construction, not by any test** — a `Promise.all` test whose handler body is wholly synchronous cannot interleave, and the comments now say so instead of citing each other. **The worker is concurrent** (`MAX_CONCURRENT_JOBS = 6`); it was serial until the whole-branch review found that `MAX_ACTIVE_JOBS_PER_USER` was therefore unreachable in production. Note the design doc's claim that "the pool's cap of 6 does double duty, because every job needs a child" is **wrong for `generate`**, which never takes a preview child — the worker-level cap is what bounds generations. **A job left `running` across a restart becomes `interrupted`, never retried**, and `interrupted` is a real state a user sees on every deploy-during-a-run: the server cannot know whether the child finished, so the UI must say the outcome is unknown rather than inventing "failed". And **`succeeded` means the request completed, not that the work passed** — a gate failure arrives as a `succeeded` job whose `result.passed` is false, which is why all five editor call sites carry an explicit `// THE TRAP` comment.

Retry is bounded and targeted, never blind. **The Anthropic SDK already retries transport failures** (`DEFAULT_MAX_RETRIES = 2`, so 3 attempts) — so it is *configured* to 2 total, not wrapped; wrapping would have multiplied to 6 while looking like the plan. Gemini is the opposite (no retries, no `Retry-After` support anywhere in the package) and genuinely needed a hand-written wrapper. Only transport failures retry, because there **no tokens were produced and nothing was billed**; a mid-SSE-body failure is deliberately *not* retried, since the model already generated those tokens and a retry would double-spend — that limitation is pinned by a test, not just a comment. `orchestrator/tests/conftest.py` now makes the suite **structurally offline** in two independent layers (poisoned SDK constructors, plus `httpx.HTTPTransport.handle_request`), because a perturbation during development made a **real, billed API call**: `config.py` loads a live key at import time and there was no global guard.

**Resume** (`POST /api/jobs/:id/resume`) re-runs a failed job from the step it failed on, and needs no new Kitaru API — reusing the `run_id` *is* the mechanism, since completed checkpoints hit the cache and the failed one has nothing cached. Empirically confirmed both halves: a reused run id is treated as a resume rather than rejected, and a **failed checkpoint's exception is not cached**. `code_version` guards it — a resume across a deploy is refused **409**, because Kitaru keys its cache on function code plus args, so a changed checkpoint re-executes while a *paired* one silently skips its side effect (that is how a real run left stale manifest entries in 5.3). The `"unknown"` sentinel counts as incompatible with everything, including itself.

The editor gained **hosted mode** behind `?project=<id>`, reached through the Vite dev server's proxy so the browser sees **one origin** — cookies flow, `SameSite=Lax` survives, no CORS, and slice 2's CSRF posture is untouched. Cross-origin was rejected for exactly that reason. Without the parameter it stays in **local mode, byte-identical**, because `compiler/scripts/preview.ts` remains unauthenticated and local and the milestone-7 Playwright suite runs against it. A **401 is its own state**, distinguishable from a job failure: reporting "regeneration failed" for a session that merely lapsed is the same lie `interrupted` exists to prevent — and the autosave path used to show **"Saved"** for writes that 401'd.

**Two operator-facing knobs and their reasoning.** `--projects-root` is now effectively single-valued: the worker **refuses to boot** unless it resolves to the orchestrator's own `generated/`, because `GENERATED_DIR` is hardcoded from the orchestrator's package location, so any other value meant a generation spent ~$1 writing a site where nothing looks for it and reported `succeeded`. Threading an `--out-dir` is the recorded follow-up. `WEBGEN_SHUTDOWN_GRACE_MS` declares the supervisor grace and derives the whole shutdown budget (`proxiedWait < watchdog < grace`), defaulting to a 10s floor and refusing anything invalid rather than clamping. At the default the proxied wait buys nothing — a regen is still killed partway and recovered via `interrupted` + resume; it only pays off when an operator declares a grace large enough to contain it.

**Known gaps, recorded rather than implied.** Fan-out-subprocess resume is unverified — the Kitaru experiment was single-process, though the real exposure is small because `page_worker.py` skips already-recorded sections from `progress.json` **before** invoking the flow, which is file-based and holds regardless of the cache. Killing a preview child mid-regen orphans the orchestrator **grandchild**, which finishes and writes a usage log nobody ingests. Six concurrent proxied jobs can consume the whole preview pool, so job traffic can 503 a user's live preview. `runOnce()` is deliberately uncapped, so a future production caller would bypass `MAX_CONCURRENT_JOBS`. The hosted server still serves **no HTML, no static assets and no login page** — production serving, a login UI and project selection are the next slice, and `docs/` still describes `--projects-root` as a free choice and the pool cap as bounding every job. Full account: decisions.md's 2026-08-06 through 2026-08-09 rows.

**Three of that paragraph's gaps are now closed, corrected here rather than rewritten** (the convention this repo uses for a stale claim — see Standing rules). *Fan-out-subprocess resume was verified in round 1*, below. *The login UI, project picker and live generation progress shipped* — in the **editor** (`LoginScreen.tsx`, `ProjectPicker.tsx`, `GenerationProgress.tsx`, `KeySettings.tsx`, hosted mode only); the **server** still serves no HTML, and production static serving is deliberately **deferred, not pending** ([docs/pending.md](docs/pending.md) D3: testers run the editor's own Vite dev server, whose proxy already carries `/api`, `/__*` and `/preview` same-origin). *The `--projects-root` and pool-cap doc claims were corrected in place* (pending.md D-1/D-3). What remains open there: H1 (the orphaned grandchild — round 1 gained **no** evidence, since it killed the orchestrator tree directly rather than a preview child), H3, H4, H5, and H6.

**Round 1 (post-slice-5 hardening) is COMPLETE**, specced in [docs/superpowers/specs/2026-08-10-post-slice-5-hardening-design.md](docs/superpowers/specs/2026-08-10-post-slice-5-hardening-design.md) and reported in [m8-live-verification.md](docs/reports/m8-live-verification.md). It ran the three features that had **never touched a live model** and all three work: **7.6 add-section** (a real new section, 184->205 semantic node ids, all seven gates incl. a real `tsc --noEmit`), **7.9 page regeneration** (every section regenerated, and one revert restored the page byte-identically across all 15 tracked files), and **fan-out-subprocess resume**. $3.94 total against a $6 authorisation, 43 `usage_event` rows, 0 unpriced.

**Resume is verified, and the proof is the money, not the log.** A generate job killed at the process-tree level mid-fan-out landed `failed` (NOT `interrupted` -- only a non-zero orchestrator exit is resumable), the resume reused the same `run_id`, re-executed **zero** of the 6 completed section checkpoints, emitted **zero** prelude events, and cost **$0.3621 against the first attempt's $1.1842 (31%)**. Kitaru **0.21.0** does not cache a failed checkpoint's exception. The run log was deliberately not trusted as the primary evidence -- it is written by the code under test -- so cost was the independent check. **The MECHANISM is deliberately NOT claimed**: the proven prelude replay happens in the PARENT process, which was never in doubt, and no `progress.json` survives in a finished project, so whether Kitaru's cache or `page_worker.py`'s file-based skip carries the section-level skip is still open.

**Two real defects were found and fixed.** `compiler/src/regen-api.ts`'s snapshot slot was **one global path, not keyed by route**, and `restoreSnapshot` deleted the target route *before* copying -- so regenerating `home` then reverting `about` **deleted `about` and replaced it with `home`'s files**, reachable over the authenticated HTTP surface. The slot stays single deliberately (it holds a whole-project `manifest.json` beside one route's page directory, so two coexisting snapshots would trade file loss for MANIFEST loss); the owning route now lives in `.regen-backup/route.txt` and is checked before any destructive step. And every generated site shipped **`<title><UNKNOWN></title>` + `"name": "unknown"`** into the handover export while the nav showed a real invented name: intake's `clarifyingQuestions` was documented for "the brief lacks a product name", but `acceptance.py` turns a clarifying question into a **hard failure**, so web-triggered generation -- which removed the human that plan approval provided -- left intake choosing between killing a ~$1 run and inventing a placeholder. Intake is now told a missing brand name is never grounds for a question, with a deterministic backstop that refuses a placeholder at the **$0.003** intake call instead of after a $1.74 generation.

**Five API facts round 1 corrected, each of which had already caused a wrong assumption.** A project's **id and its on-disk directory are different UUIDs** -- `generated/<project-id>` never exists, the directory is the run id, and `GET /api/jobs/:id` does not expose `run_id`. The success field **differs per endpoint**: `passed` for the four regen/add/edit kinds, **`ok` for `/__export`** (a gate failure is HTTP 200 + `{ok:false}` mapped to a `succeeded` job), and **neither for `generate`** (result is `{stdout}`; a gate failure lands `failed`). `/__regen-revert` takes **`{ section }`**, never `{ route }` -- that module's own header documented it wrong. `WEBGEN_MASTER_KEY` is **base64, not hex**, and a hex key *passes* the canonicality check, failing only on length. And `resolveCodeVersion()` is called **once at boot**, so commits are harmless to a running server while a **restart** refuses every later resume 409.

**A methodological warning worth more than any single finding: four of round 1's errors were in the verification plan itself, and one would have produced a false failure.** Its check was `md5sum <route>/*.tsx` -- which matches only `index.tsx`, since section components live in `<route>/sections/` -- and on a tone-only instruction **no component file changes at all**, because copy lives in `mock/*.data.ts` while the component holds structure. Followed literally, that brief files 7.9 as BROKEN. It survived only because the assertion was re-derived from three independent signals (distinct data files, mtimes in section order, six billing rows with six different token counts). **When a verification says a feature is broken, suspect the verification first.** Recorded open after round 1: H1 (killing a preview child orphans the orchestrator grandchild) gained **no** evidence, because round 1 killed the orchestrator tree directly rather than a preview child; the live archetype catalog has **27** entries where docs still say 20 (*closed 2026-08-13 as a dated in-place doc correction, pending.md D-2 — `prompts/archetypes/` holds those 27 plus the `generic-section` fallback, and §4.3 gained a fifth page archetype, `app-screen`*); `manifest.json` key order is unstable across a regen (same keys, same byte count, different hash), which defeats hash-based change detection; and the sequential page-regen loop gets **zero** prompt-cache reuse, so its cost is strictly linear in section count. Full account: decisions.md's 2026-08-10 rows.

**One defect shape this codebase keeps producing, at four different layers: `..`.** An unvalidated proxied `route` joined with `path.join`, which normalises `..`. A `runId` rail whose regex `^[A-Za-z0-9._-]+$` matched `..`, because `.` is in the character class. A project id where a single `encodeURIComponent` pass is insufficient, since WHATWG URL normalization treats `%2e` as a literal dot — `/preview/%2E%2E/x` collapses. And model-generated `route.slug`/`route.path` interpolated raw into URL paths. **Assume the fifth exists.** Any place a client- or model-influenced string reaches a path, a URL, or a spawn argument is where to look.

**It did, and the count is now ten.** The fifth was real: `manifest.ts`'s `propose` validated only `nodeId`, while `component` and `file` are **equally model-authored** (they arrive in an agent's structured output) and were persisted verbatim, then interpolated into `path.join` at six sites including the real exporter — fixed at the proposal boundary, the one place model output becomes persisted state, so all six joins are protected by construction (pending.md H2). **The tenth is OPEN and worth reading before touching the section pipeline**: `section_pipeline.write_files_repairing_images` — its own docstring calls it "the single funnel every byte of a model-authored section file passes through" — does `Path(project_dir) / rel_path` with **no validation of `rel_path`**, which is a key of the model's own `files` object; an *absolute* key discards the project root entirely, and gate 6's `out-of-boundary-write` cannot catch it because `collect_written_files` only scans `src/pages/<slug>/`. Recorded Minor **only** because the deployment model is one local tester attacking themselves; it becomes serious the day a shared host exists (pending.md M4, which also names the guard to copy: `manifest.ts`'s `validateProposalPaths`/`unsafeRelativePath`).

## Documents and their authority

Read these before writing code. Conflicts resolve top-down:

1. **[docs/codegen-contract-v1.md](docs/codegen-contract-v1.md)** — the binding contract between every code-generating agent, the editor, the override layer, and the exporter. **This document wins ALL conflicts.** Where any other doc, or code, appears to contradict it, the contract is correct.
2. [docs/agent-pipeline-spec-v1.md](docs/agent-pipeline-spec-v1.md) — orchestration DAG, agent definitions, prompt templates, token budget, Kitaru integration. May not violate the contract.
3. [docs/canvas-editor-prd-v1.md](docs/canvas-editor-prd-v1.md) — the editing surface (canvas, bridge shim, edit channels, regen UX, export flow).
4. [docs/build-plan-v1.md](docs/build-plan-v1.md) — milestone execution order and what "done" means at each.
5. [docs/build-prompts-v1.md](docs/build-prompts-v1.md) — the step-by-step build playbook (one prompt per session).
6. [docs/decisions.md](docs/decisions.md) — decision log (created in prompt P2); append a row for any call the docs don't cover.

Two files sit outside that hierarchy because they describe *status*, not spec, and both are newer than most of this file:

- **[docs/pending.md](docs/pending.md) — the living list of open work.** Read it before planning anything: every item is open or explicitly deferred *with the reason*, and its opening section states the premise that sets severity for the whole list (**friends-and-family testers each run the full stack locally, on their own machine, with their own key — there is no shared hosted instance**, which is why D1/D2's cross-tenant items are deferred rather than blocking, and what would un-defer them). Items are referenced by id throughout this file (B*, C*, D*, F*, G*, H*, K1, M*, N1, R*, U1, W1, X*).
- **[docs/runbook.md](docs/runbook.md) — the tester-facing runbook**, Docker-first, followed literally from a clean shell before it was trusted. It is the authority on *how to run the thing*; the commands below are the contributor subset. **It lived at `README.md` until 2026-09-02**, when the repo was opened to the public and `README.md` became a short front page (why the project exists, the architectural decisions, the structure, and how it was built); the runbook moved with `git mv`, so its history is intact and older records that cite `README.md` for run instructions mean this file.

## Ownership map (hard rules — from contract section 2)

Write contention is prevented by construction, not resolved. Each path has exactly one writer:

- `src/tokens/` and `src/primitives/` — **only** the Design System Agent (`tokens.css` is *derived* from `tokens.json`, never hand-edited).
- `src/shell/` (incl. `routes.ts`, the ground-truth route table) — **only** the Shell Agent.
- `src/pages/<route-slug>/` — **only** that route's Page Agent; a page agent writes nowhere else and never imports from another page's directory.
- `overrides/<route-slug>.overrides.json` — **only** the editor. No agent ever writes overrides.
- `manifest.json` — append-and-update **only** through the deterministic manifest service (never freehand, never by an LLM directly).

Target package layout (per [docs/build-plan-v1.md](docs/build-plan-v1.md) section 0): `orchestrator/` (Python 3.12, uv, pytest — the agent pipeline), `editor/` (Vite + React + TS — the canvas), `compiler/` (TS library, vitest — manifest service, token deriver, validation gates, exporter, bridge-shim protocol; shared by editor and CLI), `prompts/` (versioned archetype templates), `fixtures/` (hand-written ground-truth project), `generated/` (gitignored output workspace). **Plus `server/`, added by slice 2 and NOT in build-plan section 0** (Node 24 — requires ≥22.13 for `node:sqlite`, declared in its `engines`; vitest; no framework) — the hosted composition root and identity store. `server/data/` is gitignored and holds the identity database. Also not in section 0: **[Dockerfile](Dockerfile) + [compose.yaml](compose.yaml) + [docker/entrypoint.sh](docker/entrypoint.sh)**, one image carrying Node + Python + `uv` because **the server spawns Python at runtime**; all three files hold long measured comments that are the real documentation for their decisions.

Two facts about this layout bite in practice:

- **The real Node floor is 22.18, not `engines`' ≥22.13.** Every entry point in the repo is a `.ts` file handed straight to `node` (`server/scripts/serve.ts`, `scripts/user.ts`, `compiler/scripts/preview.ts`), and unflagged type stripping only lands in 22.18 — at 22.17 the failure is `ERR_UNKNOWN_FILE_EXTENSION: Unknown file extension ".ts"`, which names neither Node's version nor TypeScript. `≥22.13` is exactly right for what it was written for (`node:sqlite`, `stripTypeScriptTypes`) and is not the whole floor. CI and the image both pin **24**; the Dockerfile header holds the measured version table.
- **`fixtures/acme-landing` is a standalone npm project, not a workspace**, and its `node_modules` is load-bearing **at runtime, not just for tests**: the orchestrator links it into every generated project, so the preview child and the export verification build have something to resolve against. That link is *absolute*, which is why a project generated in Docker cannot be previewed from source (pending.md X1).

Two ownership rules extend the contract's list for `server/`, and both are structural rather than conventional:

- **No HTTP route may create a user.** Account creation exists *only* in `server/src/user-cli.ts`. This is what makes invite-only a property of the code rather than a feature nobody got around to. (There is no login *page* to keep a sign-up link off: the hosted server serves no HTML at all, only JSON and the preview proxy. When one is built, it carries no sign-up link.) `POST /api/generate` creates a **project**, which is allowed and is slice 5's own work; creating a *user* is not.
- **Auth lives at the HTTP boundary only.** The orchestrator CLIs, `compiler/scripts/preview.ts`, and `npm run check` must never require a login. Adding a guard to an existing handler is the wrong move; compose a new root instead.

**The login screen now exists** (`editor/src/components/LoginScreen.tsx`, hosted mode only) and carries **no sign-up link and no reset link** — both would be dead ends by design. The rule above is unchanged: it is still the *server* that serves no HTML.

**`server/src/dev-admin.ts` seeds a default `admin` / `admin` account and is NOT a violation of that rule** — read its header before touching it. It runs in the composition root at boot, with the same authority as `user-cli.ts`, and no request can reach it; it fires only on a genuinely **empty** user table (so it cannot resurrect a deleted account or re-seed after an operator `disable`), and the server binds **127.0.0.1** by default, refusing to seed at all under `--host 0.0.0.0`. It is a shipped default credential, defensible only under the local-single-tester deployment model, and **must be deleted rather than adjusted** the moment a shared host exists.

## Build philosophy (from build-plan section 0 — these govern ordering)

1. **Walking skeleton first.** One route, one archetype, one edit channel, real export, wired end to end before any breadth. Every integration risk lives at the seams (agent → manifest → shim → override → exporter); the skeleton forces every seam to exist early.
2. **Risk before breadth.** Prove the three architecture-invalidating risks *inside* the skeleton: node-ID survival across regeneration, preview/export fidelity, and prompt-cache economics. Do not defer them to polish.
3. **Deterministic before generative.** Build and test every mechanical component (manifest service, gates, exporter, shim) against the hand-written fixture project *before* any LLM generates its input. Debugging generated code through untested infrastructure means two unknowns per bug.

The `fixtures/` project is the permanent test bed for `compiler/` and shim changes — it is stable while generated output varies. It never fully dies.

## Verify commands

One root command runs the whole suite: **`npm run check`** — which is also what `.github/workflows/ci.yml` invokes, though **CI no longer runs automatically** (`workflow_dispatch` only since 2026-09-02: every automatic run had failed and the cause was never diagnosed — pending.md CI1). Treat a green `npm run check` as the gate, not a badge. — vitest for `compiler/`, `editor/` and `server/` (each package's own `test` script is `tsc --noEmit && vitest run`), then `uv run --directory orchestrator pytest`, then **both Playwright suites** (`compiler`'s shim spec, `editor`'s e2e including the invariant suite). **There is no eslint or prettier in this repo** — `tsc --noEmit` is the lint, and it already runs inside every `test`.

First-time setup in a fresh clone is **three installs, not one** (see the layout notes above for why the fixture is separate):

```bash
npm install                             # workspaces: compiler, editor, server
npm ci --prefix fixtures/acme-landing   # standalone project; load-bearing at runtime
uv sync --directory orchestrator        # Python 3.12, auto-fetched from .python-version
npx playwright install chromium         # --with-deps in CI
```

Narrower runs — reach for these instead of `npm run check` while iterating:

| Goal | Command |
|---|---|
| One package | `npm test -w compiler` (or `-w editor`, `-w server`) |
| One vitest file | `npm exec -w server -- vitest run src/jobs.test.ts` |
| One vitest case | same, plus `-t "substring of the test name"` |
| One pytest file or case | `uv run --directory orchestrator pytest tests/test_regen.py -k name` |
| One Playwright spec | `npm run test:e2e -w editor -- invariant.spec.ts` (`-g "case"` for one case) |
| Editor e2e against a **real** generated site | `WG_PROJECT_DIR=../generated/web-<uuid> npm run test:e2e -w editor` |
| The deterministic spine, end to end on the fixture | `npm run spine` (derive tokens → gates → export) |
| Gates / export / preview on any project | `npm run gates -w compiler -- <dir>` · `npm run export -w compiler -- <dir> <out> --clean` · `npm run preview -w compiler -- <dir> --port 5273` |
| Orchestrator pipeline stages | `uv run --directory orchestrator python -m orchestrator.<module>` (`acceptance`, `plan`, `design`, `shell`, `fanout`, `regenerate`, `add_section`, `run_report`, `soak`, `stress`) |

The **invariant suite** (edit → export → build → screenshot-diff, [docs/canvas-editor-prd-v1.md](docs/canvas-editor-prd-v1.md) section 7.1) is the enforcement mechanism for preview = handover (a *required* check in the sense that it gates a commit — note CI itself is manual-only, CI1); it runs on every change to the shim, the exporter, or the primitive set. `orchestrator/tests/conftest.py` makes the Python suite **structurally offline** in two independent layers — a perturbation during development once made a real, billed API call, so do not weaken it.

Validation gates (contract section 8) run after every agent and before every export via `compiler/`'s `runGates` / the `gates <dir>` CLI. Export runs typecheck + gates + a production build and **fails loudly** — a failed export never ships silently degraded code.

## Running the app

Two paths, both documented at length in [docs/runbook.md](docs/runbook.md). **Docker is the recommended path for using it; from source is the only path that can run the test suites** (the image ships no browsers).

```bash
# Docker — one command, two services on one image (server :4000, editor :5173)
docker compose up

# From source — two terminals, from the repo root
INSECURE_COOKIES=1 npm run serve -w server -- \
  --db "$PWD/server/data/identity.db" --projects-root "$PWD/generated" --bootstrap-email admin
npm run dev:hosted -w editor            # then open http://localhost:5173/
```

Five things that will otherwise cost you an hour:

- **`dev:hosted`, not `dev`.** Plain `npm run dev -w editor` is **local mode**: it talks to an unauthenticated standalone preview server on port 5273 and shows **no login screen at all**. `dev:hosted` is `vite --mode hosted`, which sets `VITE_WEBGEN_HOSTED=1` and proxies `/api`, `/__*` and `/preview` to the server on 4000 so the browser sees **one origin** (cookies flow, `SameSite=Lax` survives, no CORS). Local mode stays byte-identical and is what the milestone-7 Playwright suite runs against.
- **`WEBGEN_MASTER_KEY` is generated exactly once, and never again.** Every stored API key is AES-256-GCM ciphertext under it; a different key does not fail at boot, it fails later at decrypt time. Nothing in this repo may generate it as a convenience default — `docker/entrypoint.sh` refuses to start and explains instead, deliberately. It is **base64, not hex** (a hex key passes the canonicality check and fails only on length).
- **`--projects-root` must resolve to this repo's own `generated/`** or the server refuses to boot, because `GENERATED_DIR` is hardcoded from the orchestrator's package location. `--bootstrap-email` (or `WEBGEN_BOOTSTRAP_EMAIL`) adopts directories already sitting in `generated/`, once, on boot; without it they are ownerless, which looks exactly like an empty project list. It can never *create* an account.
- **A project's id and its on-disk directory are different UUIDs.** `generated/<project-id>` does not exist — the directory is the *run* id, and `GET /api/jobs/:id` does not expose it. Use `node server/scripts/user.ts list-projects --db …`.
- **Fan-out defaults to SERIAL, and raising it reopens a measured corruption.** `WEBGEN_FANOUT_MAX_WORKERS` unset means one page worker (`fanout.py`); at 2, two workers 424 ms apart raced Kitaru's SQLite metadata store (`journal_mode=delete`, `busy_timeout=5000`), a `commit_section_manifest` was lost, and the run shipped a `.tsx` with no manifest entry — gate 4 rejects that forever, so the site looks finished in the canvas and can **never** be exported. Making the store WAL is what would let parallelism return (pending.md K1).

Environment variables, all of them:

| Variable | Effect |
|---|---|
| `WEBGEN_MASTER_KEY` | **Required** by `server/`; base64, 32 bytes; `serve.ts` deletes it from `process.env` right after reading, because `compiler/`'s spawn sites pass no `env` and would leak it into model-authored build config |
| `INSECURE_COOKIES=1` | Drops the session cookie's `Secure` flag. Correct on plain HTTP locally; never on a real deployment |
| `WEBGEN_SHUTDOWN_GRACE_MS` | Declares the supervisor's grace and derives the whole shutdown budget (`proxiedWait < watchdog < grace`); 10s floor, refuses invalid input rather than clamping |
| `WEBGEN_BOOTSTRAP_EMAIL` | Compose equivalent of `--bootstrap-email` |
| `WEBGEN_FANOUT_MAX_WORKERS` | Unset ⇒ **1 (serial)**. See the warning above |
| `WEBGEN_HOSTED_SERVER_URL` | Where the editor's hosted-mode Vite proxy points (default `http://localhost:4000`; Docker sets `http://server:4000`) |
| `WEBGEN_USAGE_LOG` | Path the orchestrator writes its priced `usage.jsonl` rows to, so one invocation's spend is attributed to one user |
| `ANTHROPIC_API_KEY` / `GEMINI_API_KEY` + `ORCH_MODEL_PROVIDER` | `orchestrator/.env` for CLI runs. Under the server the user's own key is injected per run; both Docker services shadow `orchestrator/.env` with `/dev/null` precisely so an absent injected key cannot fall through to the operator's (python-dotenv's `override=False` has produced "the operator pays" twice) |

## Standing rules

- **Never modify `docs/` to make code pass.** The docs are the spec. If the docs are wrong, ambiguous, or in conflict, stop and flag the problem to the human with a clear report — do not resolve it by editing a doc silently.
- Red tests never cross a commit boundary. Commit after each build-prompt step with its given message prefix.
- Generated output (`generated/`) is disposable; fixtures and tests are not. Fix a generation-quality problem in the template or the contract enforcement — **never hand-patch generated files.**
- Any decision the docs don't cover → one row in [docs/decisions.md](docs/decisions.md).
- Node IDs are semantic (`home.hero.cta-primary`), never positional (`child-3`), and immutable once registered — positional IDs break regeneration.
- **A wrong claim is corrected beside the original, never rewritten.** This applies to `decisions.md`, `pending.md` and this file. A silently-rewritten record hides that a review caught something, and the log's whole value is that it can be trusted; there are entries here whose retraction is more instructive than the original claim (pending.md's own X2 withdrawal is the model).
- **`decisions.md` is append-only, and a row is dated to the day the work LANDED** — not to the incident it answers. Back-dating is a small, plausible habit that destroys the property that date order means append order (pending.md M6).
- **Close a `pending.md` item by moving it to "Recently closed" with its commit, or by recording why it stays open.** Never delete an open row.
- **Verification plans are suspect too.** When a check says a feature is broken, suspect the check first and re-derive the claim from independent signals — four of round 1's errors were in its own verification plan, and one would have filed a working feature as BROKEN. Prefer evidence the code under test does not write (money, file mtimes, billing rows) over its own run log.
- **Do not raise a concurrency knob to make something faster** without reading why it was lowered: fan-out's serial default and the preview pool's cap of 6 are both load-bearing (K1, H3).
