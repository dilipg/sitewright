import type { AboutValuesProps } from "../sections/AboutValues";

export const aboutValuesData: AboutValuesProps = {
  heading: "What we optimize for",
  body: "Every feature has to survive one question: does it shorten the path between a question and a trustworthy answer? If it only adds a surface to configure, it does not ship.",
};
