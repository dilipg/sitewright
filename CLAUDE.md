# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Product summary

An AI website-generator: a user types a one-line brief, an agent pipeline generates a complete multi-page marketing/storefront/SaaS site as typed React source, the user edits it on an infinite canvas (text, style, layout, visibility) and regenerates individual sections without losing edits, then exports a zip of developer-handover-quality code that matches the preview **pixel-for-pixel on every edited node**. The product's one unforgivable failure is preview ≠ handover; the entire architecture (stable node IDs, an override layer, a deterministic exporter) exists to prevent it.

## Current state

**Milestones 1–6 are complete.** (prompts P0–P2, 1.1–1.5, 2.1–2.4, 3.1–3.4, 4.1–4.3, 5.1–5.4). The walking skeleton works end to end (brief → generated hero → canvas edit → export; [m3-soak.md](docs/reports/m3-soak.md): 10/10 at 0.24× budget), section regeneration works via Kitaru replay-with-overrides with gate 7 + the orphan dialog + revert, and **the stop-the-line risk is retired**: the ID-survival stress suite ([m4-id-survival.md](docs/reports/m4-id-survival.md)) measured 100% auto-reattach (71/71) with zero silent drops across 20 varied regen instructions. Milestone 5 built out the full pipeline: intake + planner + plan approval (5.1), the Design System Agent (5.2), the Shell Agent + parallel page fan-out (5.3), and 5.4 — the archetype catalog grew to six (hero, feature-grid, cta-band, pricing-tiers, faq-accordion, social-proof), the editor gained its remaining P0 edit channels (text, layout, visibility) alongside the existing style channel, and the canvas became a real DOM-based, CSS-transformed infinite pan/zoom stage (one live iframe per route, virtualized to a placeholder beyond the render-ahead range — measured 60.0fps for both pan and zoom against a real 4-route generated site, [m5-canvas-perf.md](docs/reports/m5-canvas-perf.md)). The invariant suite now covers the full 4-channel × 6-archetype matrix (28 cases). 5.4 also surfaced and fixed two major, previously-invisible gaps: no generated project had real client-side routing (`main.tsx` was static, always rendering the home page regardless of URL — fixed via `react-router-dom` + route-agnostic page discovery in the scaffold every project copies from), and the exporter could not compile ANY override (text/style/layout/visibility) on a list-item node — a pricing tier, FAQ item, feature card, or testimonial — since its node id only ever exists as a template-literal pattern, never a literal JSX attribute; fixed via a new contract convention (5.5: exporter-only `className`/`childClassNames`/`hidden`/`childHidden` fields on every list item's data shape) plus matching exporter logic and archetype-template updates. A genuine data-loss bug was also found and fixed in the editor's debounced persistence (a redundant "startup save," triggered by bootstrap's own initial history hydration, could race and lose a fast first edit). See decisions.md's 2026-07-28 rows for the full account of all of the above, plus the earlier 5.1–5.3 gap cluster (gate 4 list-item ID recognition, `skipMissingCheck` scoping, tool-use schema defenses, the Kitaru `run_id` reuse lesson). `compiler/` holds the deterministic spine (token deriver, manifest service incl. replaceSection + a cross-process file lock + targeted rollback-commit, gates 1–7 + CLIs, exporter incl. list-item override compilation, bridge shim + preview server with overrides/regen/plan APIs); `editor/` covers selection, all four P0 edit channels, persisted undo/redo, the regen UX, and the infinite canvas; `orchestrator/` (Python/uv/Kitaru, needs `orchestrator/.env` with `ANTHROPIC_API_KEY`; `GEMINI_API_KEY` + `ORCH_MODEL_PROVIDER=gemini` is an opt-in escape hatch, default stays Anthropic) holds prompts/runlog/generation/regeneration/soak/stress/shell/fan-out. 5.5 built the first-ever end-to-end orchestration entry point (`orchestrator/src/orchestrator/acceptance.py`, chaining plan→approve→design→shell→fan-out→export in one process — every stage had only ever been a separately human-invoked CLI before) and a dollar-cost calculator (`orchestrator/src/orchestrator/pricing.py` — no prior measurement had ever converted tokens to dollars), then ran it live: a cheap single-page proof pass followed by 3 full 3-page acceptance runs, per [m5-acceptance.md](docs/reports/m5-acceptance.md). Cost passed comfortably on all 3 runs (~$1.7–1.8 vs. the $10 target/$15 ceiling); wall clock did not (0/3 inside the 5-minute target, 2/3 inside the 10-minute ceiling — fan-out's real per-section generation latency runs ~1.6–2.4× the docs' own ~55s/section assumption, not from prompt bloat), reported honestly rather than rounded up, per explicit human sign-off to not remediate within this milestone. The very first live, full, end-to-end runs surfaced five more real bugs, all fixed: two in the `generic-section` fallback template that only a real (non-fixture, non-6-archetype) generation run could ever trigger (contract 5.6 — a section's `<SectionName>Props` must never declare `nodeId`, it comes from a separate `NodeProps` intersection; contract 4.1 — primitives are default exports, stated nowhere before), a manifest-registration gap for `FailedSectionPlaceholder` (the first-ever live retry-exhaustion), the same template's missing list-item node-id worked example, and — most severe — an unhandled `KeyError` in `write_section_only` on a missing `sectionMeta` that could crash an entire page-worker process and silently drop every remaining section on that route, fixed via the same defensive-accessor pattern already used for a different required field. Edit-across-channels + regen was reconfirmed against real generated content (not just the fixture): all 4 P0 channels compiled correctly on a live project, and a real regen call preserved all 4 overrides with zero orphans (79.4s, over the 60s target/under the 90s ceiling). Full account: decisions.md's 2026-07-29 rows. **Milestone 6 (hardening + handover quality) is complete.** 6.1 grew the catalog to the full 20 archetypes (marketing/storefront/SaaS sets), each soaked 5 runs; 6.2 built the handover package (`HANDOVER.md` generation, a deterministic byte-identical zip, override archiving, and the editor's export UI with a loud gate-report failure panel); 6.3 covered every row of the pipeline's section-8 failure table with automated tests ([m6-failure-drill.md](docs/reports/m6-failure-drill.md)) and replaced the flat run log with a DAG timeline report (`orchestrator.run_report`: per-node status/cost/latency, drill-down to the exact rendered prompts and raw output); 6.4 ran the developer-handover trial twice against cold, no-context sessions given only an export zip ([m6-handover-trial.md](docs/reports/m6-handover-trial.md)) — **both runs met the exit criterion with zero generated components rewritten**, and the wired-up cart ran against a live REST API in each. Milestone 6 found bugs no gate could have caught, because all of them compile cleanly: two shipped defects in nine lines of generated shell (raw `<a href>` internal nav, so every click was a full page reload destroying React state; plus a dead link to the literal URL `/product/:id`), every generated site shipping the FIXTURE's identity (`<title>Acme Analytics</title>`, `"name": "acme-landing-fixture"`), an empty cart rendering a live checkout button, and `HANDOVER.md`'s central claim about swapping mock data for an API being flatly impossible as written. It also closed a real gap in gate 1, which contract section 8 defines as "imports resolve; **build passes**" but which only ever did the first half — a section that used an undeclared field passed all gates, all four page workers exited 0, and the run died at the export's typecheck after full spend; gate 1 now runs the project's own `tsc --noEmit` (1.4s, route-scoped so it is safe under parallel fan-out). Full account: decisions.md's 2026-07-30 rows.

**Milestone 7 (polish + P1s) is COMPLETE** (7.1-7.9, each written up as an executable mini-prompt in [docs/build-prompts-v1.md](docs/build-prompts-v1.md) before its implementation, per the milestone-7 process). The correctness work came first: 7.1 fixed section regeneration, which could not run at all for 19 of 20 archetypes (Kitaru substitutes `${...}` as env vars in replayed values, and contract 5.2 requires `${nodeId}` in list-item ids) plus a second bug where `recorded_exec_id` replayed whichever section finished last rather than the one requested; 7.2 scoped gates 1/2/3/5/6 to the route under generation so a worker cannot burn its retry budget on a sibling's half-written page; 7.3 measured wall clock and found 5.5's diagnosis wrong -- per-section latency is 27s, not the 88-132s inferred, the run completes in 286s inside the 5-minute target, and the real costs are a ~149s sequential prelude plus ~13s/section of contention inflation ([m7-wall-clock.md](docs/reports/m7-wall-clock.md)); 7.4 closed the four two-trial handover findings (money as numbers behind `src/lib/format.ts`, a `Notice` primitive for runtime status, per-item pending, derived-type guidance). Then the P1 features, cheapest blast radius first: 7.8 responsive read-only preview (read-only because an override carries no breakpoint, contract 6.1); 7.7 image replace as the TEXT channel with key `src` (PRD 3.5: content, not style -- not a new channel); 7.5 section reorder, the first PAGE-level override channel, whose `nodeId` is a route slug because reordering belongs to the page rather than to any section -- strict by design (a partial order is a hard export failure, since an omitted section would silently vanish), running last of all channels (visibility removes JSX first), leaving id-less children like `FailedSectionPlaceholder` in place, and reordering the REAL DOM in preview rather than faking it with flex `order`, which would change the formatting context and break preview = handover invisibly; 7.9 page-level regeneration as a sequential loop over the section path (there is no page-level execution to replay, per 7.1) with one route-wide snapshot taken before the first section, so one revert restores the page; 7.6 add-a-section last, since it spends budget per use and mutates the site plan -- a first generation (not a replay: the section never existed) appended to the source and positioned by a `sectionOrder` override, which is why it needed 7.5. Milestone 7's own findings are in decisions.md's 2026-08-02 rows; three are worth knowing: reordering `home` in the invariant suite flipped two unrelated cases onto different pixel boundaries, because a ghosted node legitimately shifts everything below it between preview and export, so the reorder case lives on the one route with no visibility case; the `+` affordance first shipped as a full-width invisible strip that swallowed clicks on section content; and mock regen hardcoded `Hero`, so a page loop would have rewritten one file six times while reporting six sections done. **Two verification gaps are recorded deliberately rather than implied: neither 7.9 nor 7.6 was run against a live model** -- both wrap flows already proven live, and both are covered by unit tests plus mock-mode e2e (7.6's ends in a real export, so all seven gates run). **Also open for a human call: [docs/build-plan-v1.md](docs/build-plan-v1.md) and [docs/build-prompts-v1.md](docs/build-prompts-v1.md) both narrate a "full 15-primitive set", now stale after 7.4 added `Notice` (16).** The contract is not violated -- contract 4.1 says "Minimum v1 set" -- so this was flagged rather than silently edited, per the standing rule.

**Post-milestone-7 work is organised as five SLICES turning this local single-user tool into a hosted multi-user app, specced in [docs/superpowers/specs/2026-08-04-accounts-byok-tenancy-design.md](docs/superpowers/specs/2026-08-04-accounts-byok-tenancy-design.md) (13 decisions, 4 named accepted risks). Slices 1 and 2 are merged.** Slice 1 (prompt-driven editing) added `/__edit-prompt`: the user types an instruction instead of clicking, and an edit agent returns override operations. Slice 2 (accounts) added a **fourth workspace package, `server/`** — the one thing a fresh session is least likely to look for, because milestones 1–7 never mentioned it. `server/` is a **separate composition root**, not a change to the existing one: `compiler/scripts/preview.ts` stays unauthenticated and local, all pre-existing tests stay untouched, and auth is added by composing a different root rather than by editing any handler. It holds a SQLite identity store on `node:sqlite` (no `better-sqlite3`; WAL, foreign keys on, `busy_timeout` because the CLI and the server are two processes on one file), argon2id passwords, server-side sessions, a hand-rolled `node:http` route table where **the table IS the allowlist** (an unregistered path is unreachable, not merely unguarded), `POST /api/login` + `/api/logout` + `GET /api/me`, and an operator CLI that is the **only** way an account comes into existence — invite-only is structural, not an unimplemented feature. Three properties are load-bearing and easy to break by accident: the auth failure response is **uniform** across unknown-email / wrong-password / disabled (else the form is an enumeration oracle, which is why a real argon2 verification runs even when no user matched); revocation is **immediate** (the CLI flags the account *and* hard-deletes its session rows, while `resolveSession` independently re-reads the user and rejects a disabled one — either alone would do, both are deliberate); and login **requires `Content-Type: application/json`**, which is what closes login-CSRF, since `SameSite=Lax` only guards requests that *send* the cookie and login *sets* one. The whole-branch review found four defects no per-task review could see, all fixed — worth knowing because they are the shape of bug this package attracts: a malformed `Host` header **killed the entire process** (the `new URL()` call sat outside the try, so the rejection went unhandled); `busy_timeout` was 0, so an emergency `disable` could silently fail against a running server; login was CSRF-able; and the identity database was not gitignored. Fixing the first then introduced a fifth — module-scope `uncaughtException` handlers swallowed *startup* failures and exited 0 reporting success — so those handlers now arm only inside the `listen` callback. Full account: decisions.md's 2026-08-03 and 2026-08-04 rows. **Slices 3 (BYOK per-user API keys), 4 (tenancy + spend-cap enforcement) and 5 (web-triggered generation) are specced but not built.** Two things to know before starting them: the spec's $10/24h rolling cap needs **per-user** spend accounting, and today `cost_for_run` cannot see edit-agent spend at all (edits log to `usage.jsonl`, which carries no run id, while `cost_for_run` reads the per-run log) — so that gap belongs to slice 4 rather than being a standalone fix; and slice 5 needs a job model, since a web-triggered generation outlives its request.

## Documents and their authority

Read these before writing code. Conflicts resolve top-down:

1. **[docs/codegen-contract-v1.md](docs/codegen-contract-v1.md)** — the binding contract between every code-generating agent, the editor, the override layer, and the exporter. **This document wins ALL conflicts.** Where any other doc, or code, appears to contradict it, the contract is correct.
2. [docs/agent-pipeline-spec-v1.md](docs/agent-pipeline-spec-v1.md) — orchestration DAG, agent definitions, prompt templates, token budget, Kitaru integration. May not violate the contract.
3. [docs/canvas-editor-prd-v1.md](docs/canvas-editor-prd-v1.md) — the editing surface (canvas, bridge shim, edit channels, regen UX, export flow).
4. [docs/build-plan-v1.md](docs/build-plan-v1.md) — milestone execution order and what "done" means at each.
5. [docs/build-prompts-v1.md](docs/build-prompts-v1.md) — the step-by-step build playbook (one prompt per session).
6. [docs/decisions.md](docs/decisions.md) — decision log (created in prompt P2); append a row for any call the docs don't cover.

## Ownership map (hard rules — from contract section 2)

Write contention is prevented by construction, not resolved. Each path has exactly one writer:

- `src/tokens/` and `src/primitives/` — **only** the Design System Agent (`tokens.css` is *derived* from `tokens.json`, never hand-edited).
- `src/shell/` (incl. `routes.ts`, the ground-truth route table) — **only** the Shell Agent.
- `src/pages/<route-slug>/` — **only** that route's Page Agent; a page agent writes nowhere else and never imports from another page's directory.
- `overrides/<route-slug>.overrides.json` — **only** the editor. No agent ever writes overrides.
- `manifest.json` — append-and-update **only** through the deterministic manifest service (never freehand, never by an LLM directly).

Target package layout (per [docs/build-plan-v1.md](docs/build-plan-v1.md) section 0): `orchestrator/` (Python 3.12, uv, pytest — the agent pipeline), `editor/` (Vite + React + TS — the canvas), `compiler/` (TS library, vitest — manifest service, token deriver, validation gates, exporter, bridge-shim protocol; shared by editor and CLI), `prompts/` (versioned archetype templates), `fixtures/` (hand-written ground-truth project), `generated/` (gitignored output workspace). **Plus `server/`, added by slice 2 and NOT in build-plan section 0** (Node 24 — requires ≥22.13 for `node:sqlite`, declared in its `engines`; vitest; no framework) — the hosted composition root and identity store. `server/data/` is gitignored and holds the identity database.

Two ownership rules extend the contract's list for `server/`, and both are structural rather than conventional:

- **No HTTP route may create a user.** Account creation exists *only* in `server/src/user-cli.ts`. The login page carries no sign-up link. This is what makes invite-only a property of the code rather than a feature nobody got around to.
- **Auth lives at the HTTP boundary only.** The orchestrator CLIs, `compiler/scripts/preview.ts`, and `npm run check` must never require a login. Adding a guard to an existing handler is the wrong move; compose a new root instead.

## Build philosophy (from build-plan section 0 — these govern ordering)

1. **Walking skeleton first.** One route, one archetype, one edit channel, real export, wired end to end before any breadth. Every integration risk lives at the seams (agent → manifest → shim → override → exporter); the skeleton forces every seam to exist early.
2. **Risk before breadth.** Prove the three architecture-invalidating risks *inside* the skeleton: node-ID survival across regeneration, preview/export fidelity, and prompt-cache economics. Do not defer them to polish.
3. **Deterministic before generative.** Build and test every mechanical component (manifest service, gates, exporter, shim) against the hand-written fixture project *before* any LLM generates its input. Debugging generated code through untested infrastructure means two unknowns per bug.

The `fixtures/` project is the permanent test bed for `compiler/` and shim changes — it is stable while generated output varies. It never fully dies.

## Verify commands

One root command runs every package's test suite: **`npm run check`** (npm workspaces for `compiler/` and `editor/`, then `uv run --directory orchestrator pytest`). CI runs exactly this command. Per-package tools: `orchestrator/` → pytest (Python 3.12 via uv); `compiler/` and `editor/` → vitest; editor/shim end-to-end → Playwright (from milestone 2). The **invariant suite** (edit → export → build → screenshot-diff, [docs/canvas-editor-prd-v1.md](docs/canvas-editor-prd-v1.md) section 7.1) is a required CI check and the enforcement mechanism for preview = handover; it runs on every change to the shim, the exporter, or the primitive set.

Validation gates (contract section 8) run after every agent and before every export via `compiler/`'s `runGates` / the `gates <dir>` CLI. Export runs typecheck + gates + a production build and **fails loudly** — a failed export never ships silently degraded code.

## Standing rules

- **Never modify `docs/` to make code pass.** The docs are the spec. If the docs are wrong, ambiguous, or in conflict, stop and flag the problem to the human with a clear report — do not resolve it by editing a doc silently.
- Red tests never cross a commit boundary. Commit after each build-prompt step with its given message prefix.
- Generated output (`generated/`) is disposable; fixtures and tests are not. Fix a generation-quality problem in the template or the contract enforcement — **never hand-patch generated files.**
- Any decision the docs don't cover → one row in [docs/decisions.md](docs/decisions.md).
- Node IDs are semantic (`home.hero.cta-primary`), never positional (`child-3`), and immutable once registered — positional IDs break regeneration.
